# Evala AI

This folder is reserved for off-chain automation and batch jobs (e.g., scheduled summarization, analytics).

For now, the primary AI entrypoint is the Next.js API route at `frontend/app/api/summarize/route.ts`.
